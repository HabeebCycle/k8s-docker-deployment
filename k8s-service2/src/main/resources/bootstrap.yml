spring:
  application:
    name: k8s-service2

  cloud:
    config:
      discovery:
        enabled: true
        serviceId: CONFIGSERVER
      failFast: true

eureka:
  client:
      serviceUrl:
          defaultZone: ${EUREKA_SERVER:http://localhost:8761/eureka}

server:
  port: 9091